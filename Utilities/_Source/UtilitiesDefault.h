#ifndef _UTILITIES_DEFAULT_H_
#define _UTILITIES_DEFAULT_H_

#include "UtilitiesTypes.h"

#define SHOW_FPS
#define ENABLE_FREE_CAMERA
#define ENABLE_COLLISION_WIREFRAME
#define ENABLE_OCTREE_DISPLAY
#define ENABLE_WAY_POINT_DISPLAY

#define MAX_FONT_FILENAME_INPUT		128
#define MAX_INPUT_LEN							128
#define MAX_FILENAME_INPUT				256

namespace Utilities
{
	const float PI = 3.14159265359f;

	const UINT8 MAX_UINT8 = 0xFF;
	const UINT16 MAX_UINT16	= 0xFFFF;
	const UINT32 MAX_UINT32	= 0xFFFFFFFF;
	const UINT32 FONT_HEIGHT = 25;

	const UINT32 DEFAULT_MEMORY_POOL_SIZE = MAX_UINT16;
	const UINT32 DEFAULT_ID_SIZE = MAX_UINT16;

	const D3DCOLOR TRANSPARANT = D3DCOLOR_ARGB( 0, 0, 0, 0 );
	const D3DCOLOR WHITE = D3DCOLOR_ARGB( 255, 255, 255, 255 );
	const D3DCOLOR BLACK = D3DCOLOR_ARGB( 255, 0, 0, 0 );
	const D3DCOLOR RED = D3DCOLOR_ARGB( 255, 255, 0, 0 );
	const D3DCOLOR GREEN = D3DCOLOR_ARGB( 255, 0, 255, 0 );
	const D3DCOLOR BLUE = D3DCOLOR_ARGB( 255, 0, 0, 255 );
	const D3DCOLOR YELLOW = D3DCOLOR_ARGB( 255, 255, 255, 0 );
	const D3DCOLOR PURPLE = D3DCOLOR_ARGB( 255, 255, 0, 255 );
	const D3DCOLOR CYAN = D3DCOLOR_ARGB( 255, 0, 255, 255 );

	const D3DXVECTOR3 RIGHT_DIRECTION = D3DXVECTOR3( 1.0f, 0.0f, 0.0f );
	const D3DXVECTOR3 UP_DIRECTION = D3DXVECTOR3( 0.0f, 1.0f, 0.0f );
	const D3DXVECTOR3 FORWARD_DIRECTION = D3DXVECTOR3( 0.0f, 0.0f, 1.0f );

	const float DEFAULT_DEBUG_SPHERE_RADIUS = 10.0f;
}

#endif	// #ifndef _UTILITIES_DEFAULT_H_